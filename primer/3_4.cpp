#include <iostream>
#include <string>

using namespace std;

int main () {
    // string s1, s2;
    // getline(cin, s1);
    // getline(cin, s2);

    // if (s1 > s2)
    // {
    //     cout << s1;
    // } else {
    //     cout << s2;
    // }
    
    char A = 'A';
    char a = 'a';
    cout << "the ascii of A and a is " << (int)A << " and " << (int)a << endl;

    return 0;
}